- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig:
#    ~/.vim:
#    ~/.vimrc:

- create:
    - ~/Work
    - ~/.vim/undo-history

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]
